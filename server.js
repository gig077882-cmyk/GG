/**
 * Entry point for the Telemost server.
 *
 * All implementation lives in `src/server/`; this file just imports the
 * bootstrap module so existing scripts (`npm start`, `node server.js`) keep
 * working without changes.
 * @module server
 */

require("./src/server/index");
